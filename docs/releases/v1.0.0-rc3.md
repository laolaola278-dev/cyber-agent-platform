# v1.0.0-rc3 — Certified Release Candidate

**Phase 28.5-RC2 certified (14/14 gates PASS) · Kubernetes Production Operations baseline (Phase 28.6)**

- **Baseline commit:** `705bdd2` (race hardening + general CI green + Trivy clean)
- **Release hygiene:** version aligned to `1.0.0-rc3` across VERSION, backend
  pyproject/uv.lock, frontend package.json/package-lock, sdk, Helm chart,
  image labels (`8be447b`).
- **Race closure (Phase 28.5-RC2):**
  - cancel/complete + renew/reclaim are DB-atomic conditional UPDATEs; the
    PG deterministic barrier tests + ≥500-round stress (invalid=0,
    split-brain=0) certify the linearization semantics.
  - Full regression same-SHA ×3: `190 passed / 4 skipped / 1 deselected`
    (main push, release tag, rerun).
  - General ci.yml green: backend (Python 3.13, 1047 tests) / packaging /
    frontend / image-and-security (Trivy HIGH/CRITICAL = 0).
- **Security:** Linux security certification 28 passed; browser gate bound to
  a real OCI Chromium runtime test; `worker_control_plane_isolation =
  NOT_CERTIFIED` (docker.sock mounted) — the target for Phase 28.6.
- **500-run release certification:** `BENCH durability n=500` (0 loss / 0
  stuck) + `BENCH lab n=40` (COMPLETE + MinIO blobs) PASS.

See `docs/CHANGELOG.md` and the Phase 28.5-RC2 report
(`backend/docs/phase_28_5_rc2_final_blocker_closure_report.md`).
