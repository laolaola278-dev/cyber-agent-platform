# v1.0.4-rc1 — Console Completion RC

> **RC anchor for the console-completion line.** The code at this anchor
> versus the v1.0.3 release (`6a79c1e`) is one commit (`9dbe60f`):
>
> - **Frontend console completion** — the remaining 7 inline views
>   (Dashboard, Investigations, Acquisitions, Approvals, Plugins, Access,
>   Settings) are extracted verbatim into `pages/`; `App.tsx` shrinks
>   672 -> 157 lines and is now a pure shell (menu + global health/error
>   banner). All 16 views are components. Navigation semantics unchanged
>   (still the `page` state switch, not a router). Projection views keep the
>   App-level data fetch (props in); stateful views own their actions
>   verbatim with `App.useApp()` message feedback and the shared
>   `errorMessage()` helper.
> - **CI**: actions bumped to Node 24 majors (checkout v6, setup-python v6,
>   setup-node v6) to clear the Node 20 deprecation warnings.
> - **Docs**: `docs/quality/coverage-matrix.md` created (governance P2) and
>   `docs/roadmap.md` After-1.0.0 section enumerates the four candidate
>   tracks.
>
> `scripts/release/classify_diff.py 6a79c1e 9dbe60f` classifies the frontend
> paths as `production_runtime` (fail-closed by design), so the v1.0.3
> certification is NOT inherited — this rc re-certifies from scratch.
> No backend, API, schema, migration or deployment change beyond version
> carriers.

## Certification plan (this anchor)

- `ci.yml` — green on `9dbe60f` (pre-bump CI run)
- `cap-linux-certification.yml` — green on `9dbe60f`
- `cap-k8s-certification.yml` — green on `9dbe60f`
- GA Certification (strict) — to be dispatched on this anchor after the
  7200s reliability soak completes; target 40/40, `full_ga_certified=true`

## Known limitations (carried forward, unchanged)

- Response EDR/WAF/Firewall providers remain mock-only.
- Zeek TSV is unsupported (design-level rejection with remediation guidance).
- 24-hour soak not executed (certified soak is 7200s).
- SLOs remain candidates pending a full month of production data
  (`outputs/cap-cert-ga/slo-candidates.json`).
