# v1.0.6-rc1 — Post-1.0.5 Delivery Audit Anchor

> **RC anchor for the 1.0.6 development line.** v1.0.5 is published and
> immutable (`v1.0.5` -> `32ec298`, GitHub Release "Cyber Agent Platform v1.0.5",
> 2026-09-07), so the post-release work below cannot ship as 1.0.5; the version
> policy requires a new RC. This commit is a pure version-metadata bump over the
> audited tree, so the audited content and the anchor are the same runtime.
>
> `python scripts/release/classify_diff.py 32ec298 <pre-anchor>` reports
> `64 files, runtime_affecting=true, RECERTIFICATION_REQUIRED`
> (production_runtime 48 | test_harness 6 | docs 5 | dependency 2 |
> ci_workflow 1 | database 1 | deployment 1). The v1.0.5 certification is
> therefore **not inherited** — this anchor re-certifies.

## What is on this line

Runtime-affecting:

- **Asset search 500 on PostgreSQL** (`41bbc49`) — `AssetRepository.search`
  applied `statement.distinct()` unconditionally, so its count query became
  `SELECT DISTINCT` over every asset column and PostgreSQL has no equality
  operator for the `json` columns. `GET /assets` answered 500; SQLite accepted
  both shapes, which is why 1200 unit tests never saw it. The tag filter is now
  an id semi-join and the `DISTINCT` is gone.
- **`CAP_ZAP_API_KEY` was documented but never consumed** (`41bbc49`) —
  `Settings` had no matching field and `model_config` uses `extra="ignore"`, so
  `MemorySecretProvider` could never hold the `zap-api-key` reference
  `get_zap_api_key` resolves. Every route reaching `AssessmentService` failed
  with `SECRET_NOT_FOUND`, and because `POST /incidents` depends on
  `PlaybookService` -> `AssessmentService`, **creating an incident was
  impossible on a fresh deployment**.
- **Audit attribution** (`41bbc49`) — API-path audit events and
  `Asset.deleted_by` recorded the literal `api-user` instead of the
  authenticated principal.
- **Workflow approval gate** (`89eac38`) — `ApprovalNodeHandler` was the Phase 3
  placeholder that always returned `WAITING`, so a playbook with a human gate
  could be started and never finished, and `POST /workflow/run/{id}/resume` just
  parked it again. A reviewer decision recorded through
  `POST /workflow/run/{instance_id}/decision` now opens (`APPROVED`) or
  terminates (`REJECTED`, with the reviewer's reason) the run; the decision is
  persisted on the run context and a gate cannot be answered twice.
- **Authorization mapping** (`89eac38`) — that route resolves to
  `approval.decide`, the permission that already governs response-plan
  approvals, instead of falling through to `platform.manage`.
- **Error text shown to operators** (`89d494c`) — the console read only
  FastAPI's `detail` string, so every platform-envelope failure showed axios's
  own status text and every validation failure showed `[object Object]`.
  `errorMessage()` now reads all three shapes.
- **Console completeness** (`5eeb1b8`, `b2c12c5`, `89d494c`, `8812381`,
  `7a738fc`) — real loading/error/empty states on the shell-owned views, the
  acquisition resume/cancel lifecycle wired, the Approval Center and Plugin
  inventory completed, five unnamed action columns labelled, the Playbook
  document card relabelled to the JSON it renders, and the incident assignment
  dialog no longer fires a request that changes no field.
- **Version carrier that lied** (`1a55df0`) — `docker-compose.yml` defaulted
  `APP_VERSION` to a literal five releases old, and the console now displays
  `/health`'s version, so a compose deployment self-reported the wrong release.
- **Console read permissions** (`c8c170f`) — eight API prefixes had no entry in
  the authorization map and fell through to `platform.manage`, so the default
  `read-only` console got 403 on Acquisitions, Agents, Tasks, Workflow, Registry,
  Capabilities and Runtime, and the only workaround was a blanket grant. Reads
  now resolve to per-resource read/execute permissions, and a console endpoint
  left unmapped is a test failure rather than a silent privilege widening.
- **A healthy long-running acquisition could be cancelled** (`d9a2e01`, `3ebca44`,
  `c52dcb9`) — the execution-lease heartbeat renewed on the same session as the
  operation it was protecting, and any renewal error other than a genuine
  ownership loss (SQLite's single-writer lock under contention being the common
  one) stopped the heartbeat silently, so the lease lapsed, fencing correctly
  rejected the stale commit, and the run was finalised `CANCELLED` while nothing
  was wrong with it. The runtime now derives a renewal-only session where the bind
  can serve one, retries a transient renewal on the next tick, shares one named
  renewal cadence between both call sites, and stops renewing at all where a
  second connection does not exist. Fencing semantics are unchanged.
- **Certification gate portability** (this line) —
  `scripts/release/classify_diff.py` decoded git output with the platform
  encoding; on a GBK Windows console any non-ASCII byte in a diff killed the
  reader thread and the gate died with an unrelated `AttributeError`. It now
  decodes UTF-8 explicitly and fails loudly rather than reporting "no changed
  lines".

Schema: **no `backend/alembic/versions` file differs from v1.0.5** — the
migrated schema is unchanged, so PostgreSQL migration evidence from the v1.0.5
line applies to the schema itself, while the runtime that reads it must still be
re-certified at this anchor.

## Tests added on this line

`backend/tests/test_release_version_consistency.py`,
`test_asset_search_postgresql.py`, `test_audit_operator_attribution.py`,
`test_backup_manifest_verifier.py` (DR restore gate proven hermetically),
`test_workflow_approval_gate.py`, `test_release_diff_classifier.py` (UTF-8
regression), plus the console suite `frontend/src/test/*` (29 tests over the
real axios adapter, unstubbed request = failure).

Added while certifying the candidate:
`test_release_publication_gate.py` (executes `release.yml`'s certification gate
against canned Actions-API answers), `test_ga_report_preview_mode.py` (the GA
generator's three decision modes, run as a real process),
`test_certification_workflow_contract.py` (workflow definitions asserted like
code, including the release-layer gate step executed inline),
`test_quality_gate_parity.py` (`make lint`/`make check` bound to the CI steps,
and every CI-excluded test required to have a home that collects it),
`test_settings_dotenv_hermeticity.py` (the suite cannot be steered by a
developer `.env`), `test_phase_28_7_ga_heartbeat_invariant.py` (lease-heartbeat
isolation, transient-renewal retry, renewal cadence contract, and the
PostgreSQL row-locking variant on a real server), `test_rbac_permission_mapping.py`
(an unmapped console endpoint fails the build), `test_migration_catalogue.py`
(the migrated schema against what the revisions and models promise), and
`test_sandbox_socket_boundary.py` (deployment sockets and the prose that
describes them cannot drift apart).

## Measured at the audited tree (Windows, no container runtime)

| Gate | Result |
|------|--------|
| backend suite (CI invocation) | 1224 passed / 123 skipped / 1 deselected / 0 failed |
| coverage | 91.85% (gate 90%) |
| console | 29 passed, `tsc` + `eslint --max-warnings=0` clean |
| production build | success, entry chunk 487 kB (limit 550) |
| live PostgreSQL HTTP E2E | 37/37 PASS (RBAC fail-closed, invalid input 4xx, audit trail, separation of duties, approval gate, restart durability) |
| built console in a real browser | renders live aggregates; 0 console errors |

Full table with commands: `docs/quality/final-delivery-audit-2026-09-19.md`.

## Certified at `c52dcb9`

The six items below were the release condition; each is now green on that SHA, on
GitHub-hosted Linux runners, with the evidence bound to the commit it certifies:

| # | Condition | Result |
|---|-----------|--------|
| 1 | CI on the candidate SHA | five jobs success (`35439324779`): lint, unit + coverage ≥ 90%, frontend typecheck/build, Alembic head on PostgreSQL 16, image build + Trivy |
| 2 | CAP Linux Certification, critical skips = 0 | `full-certification` + `cap-production-certification` 12/12 gates each, 193 regression tests, 500-run OCI benchmark, 100-run kill-9 HA gate, adversarial security suite; PostgreSQL matrix green on 15/16/17 (`35439343387`) |
| 3 | K8s Certification | 33/33 gates, 0 not_run (`35439344924`) |
| 4 | Images by digest + SBOM + provenance + Trivy policy | built and scanned in CI (`image-and-security`, `supply-chain`); registry digests exist only once a tag is published |
| 5 | Real Redis/MinIO, real nginx route, Helm install + upgrade | object store on the digest-pinned Quay MinIO, console route through the shipped nginx image, Helm upgrade with data in place (`§10/§12/§17` of the report) |
| 6 | GA gates classified inherited vs re-run, with reasons | `35445391334`, `ga_strict=true`: **40/40 PASS**, 0 skipped, soak evidence resolved by `head_sha` from `35439341789` |

Reliability: the 7200 s soak with chaos recorded 0 HTTP errors, 0 downtime
seconds, 480/480 healthy ticks, 11 worker pods killed mid-run, upgrade 6.1 s and
rollback 0.5 s under load.

Full evidence, per-gate tables and the open findings:
`docs/quality/cap-1.0.6-rc1-release-certification-2026-09-19.md`. The local
figure above (`1224 passed / 123 skipped`) predates the candidate; the candidate's
own local run is in that report, and no local Windows result is claimed as
production certification.

## What publication still requires

1. **An explicit authorization.** Nothing here was tagged, published or pushed:
   no `v*` tag, no GitHub Release, no image or tag overwritten.
2. Nothing left in the image graph: F-7 closed it on this line, so the release
   publishes all five CAP images the chart can deploy and `release-image-completeness`
   refuses a partial set. `docs/known-issues.md` records what changed.
3. A v1.0.6 → v1.0.5 rollback exercise against released data, which cannot be
   performed before 1.0.6 exists.
4. The migration-naming drift and the tag-pinned base images recorded in the
   report's open findings, each deliberately left out of a release candidate.
